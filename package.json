{
  "name": "magicblock-permissionless-prediction-market",
  "version": "0.1.0",
  "description": "Permissionless private prediction market on Solana using MagicBlock / PER",
  "private": true,
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "test:skip-local": "anchor test --skip-local-validator",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "keys:sync": "anchor keys sync",
    "clean": "rm -rf target .anchor node_modules/.cache"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@solana/web3.js": "^1.98.0",
    "@solana/spl-token": "^0.4.13",
    "chai": "^5.1.2"
  },
  "devDependencies": {
    "@magicblock-labs/ephemeral-rollups-sdk": "^0.15.3",
    "@types/chai": "^5.0.1",
    "@types/mocha": "^10.0.10",
    "bs58": "^6.0.0",
    "dotenv": "^16.3.1",
    "mocha": "^10.8.2",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.7.2"
  }
}
